#include "proxy.h"

int main(int argc, char ** argv){

    //int capacity = atoi(argv[1]);
    //Cache proxyCache(capacity);
    //char * c = "12345"
    Proxy myProxy;

    myProxy.handleProxy();
    
}
